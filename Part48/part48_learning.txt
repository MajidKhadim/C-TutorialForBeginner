In this part we learnt about:
	-- Customer is the Type and fields,Properties and methods are type members
	-- So, in general classes,structs,interfaces,delegates are called as types and fields,properties constructors,methods etc that normally reside in a type are as type members
	-- Type members can have all types of access modifiers and types can only have 2 access modifiers (public and internal)